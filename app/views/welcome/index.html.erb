<div class="wrapper wrapper_padding clearfix">
    <div class="col-1 about homepage_content">
        <h2>About</h2>
        <hr>
        <p>I am a Ruby on Rails developer living out in Phoenix, AZ</p>
        <p>About a year ago I left my stable career in the insurance industry and turned my hobby of programming into a professional career.</p>
        <p>Feel Free to shoot me a message!</p>
    </div>
    <div class="col-2 homepage_content">
        <h2>Blog</h2>
        <hr>
        <% @posts.each do |post| %>
            <h3><a href="#"><%= link_to post.title, post %></a></h3>
            <p class="date"><%= post.created_at.strftime("%A, %b %d") %></p>
        <% end %>
    </div>
    <div class="col-3 homepage_content">
        <h2>Projects</h2>
        <hr>
        <% @projects.each do |post| %>
            <%= image_tag post.image.url(:small) %>
            <h3><a href="#"><%= link_to post.title, post %></a></h3>
            <p class="date"><%= post.created_at.strftime("%A, %b %d") %></p>
        <% end %>
    </div>
</div>